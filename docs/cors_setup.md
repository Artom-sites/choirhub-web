# Виправлення помилки завантаження PDF (CORS для Cloudflare R2)

Ваші PDF-файли зберігаються на **Cloudflare R2**, а не на Firebase Storage.
Щоб мобільний додаток міг їх завантажувати, потрібно налаштувати CORS в панелі Cloudflare.

## Інструкція

1.  Відкрийте **[Cloudflare Dashboard](https://dash.cloudflare.com/)**.
2.  Зліва виберіть **R2 Object Storage**.
3.  Натисніть на ваш бакет (ймовірно `choir-pdfs` або схоже).
4.  Перейдіть на вкладку **Settings**.
5.  Прокрутіть вниз до секції **CORS Policy**.
6.  Натисніть **Add CORS policy** або **Edit**.
7.  Вставте такий JSON:

    ```json
    [
      {
        "AllowedOrigins": ["*"],
        "AllowedMethods": ["GET", "HEAD"],
        "AllowedHeaders": ["*"],
        "MaxAgeSeconds": 3600
      }
    ]
    ```

8.  Натисніть **Save**.

Після цього мобільний додаток зможе завантажувати PDF без помилки CORS! 🎉
